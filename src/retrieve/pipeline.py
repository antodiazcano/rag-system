"""Script to build the complete RAG pipeline: transformation + search + reranking +
include context."""


def rag_pipeline() -> str:
    """Executes all the RAG pipeline to obtain the final prompt.

    Returns:
        Modified prompt using RAG.
    """

    return "hello"
